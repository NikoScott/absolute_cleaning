<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<nav class="navbar navbar-expand-lg bg-secondary-subtle fixed-top nav-shadow">
    <div class="container-fluid justify-content-end">
    <a class="navbar-brand" href="#"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;"> </font></font></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
        <div class="collapse navbar-collapse justify-content-center" id="navbarSupportedContent">
            <ul class="navbar-nav text-center align-items-center text-body-secondary mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link m-3 text-primary-emphasis" aria-current="page" href="{{ path('app_home')}}"> Accueil </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link m-3 text-primary-emphasis" aria-current="page" href="#"> Qui sommes nous ? </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link m-3 text-primary-emphasis" aria-current="page" href="#"> Nos réalisations </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link m-3 text-primary-emphasis" aria-current="page" href="#"> Devis gratuit </a>
                </li>

                {% if is_granted('ROLE_ADMIN') %}      
                    <a href="{{path('admin')}}" class="btn btn-info"> Back office </a>
                {% endif %}
            </ul>
        </div>
      </div>
    </nav>